const fs = require("fs");
const { Module } = require("module");

const forDeletion = reqiure("../utils/variables");
const forDeletionID = require("../utils/variables");
const tabela_kerkim = require("../utils/variables");
const tabela_fushat = require("../utils/variables");
//------------------ fund variablat 1 here----------------

//--------------------------------------------
//OK---console.log("tabela_fushat", tabela_fushat);
//------------definimi  i fushave per update ne tabele ------------
//var let_request;

//----------------------
/*
fs.writeFile(
  "./config/body_params_Let_Get.txt",
  JSON.stringify(item_field_get),
  function (err) {
    if (err) throw err;
  }
);

//console.log("ids ", tabela_rregull);
*/
